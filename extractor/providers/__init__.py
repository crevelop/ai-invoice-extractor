"""Model providers, one module each, behind the same call_structured()
signature. Anthropic today; openai.py lands in build step 7 so the eval
table can compare providers without touching the engine."""
